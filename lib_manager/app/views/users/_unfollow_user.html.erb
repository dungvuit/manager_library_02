<%= form_for :unfollow_book, url: user_follow_user_path(user_id: current_user.id,
  id: @user.id), method: :delete, remote: true do |f| %>
  <%= f.submit t("views.user.shows.btn_unfollow"), class: "btn btn-danger" %>
<% end %>
