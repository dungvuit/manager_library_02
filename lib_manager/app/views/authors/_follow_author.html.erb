<%= form_for :following_author, url: user_follow_authors_path(current_user),
  remote: true do |f| %>
  <%= hidden_field_tag :author_id, @author.id %>
  <%= f.submit t("views.authors.btn_follow"), class: "btn btn-success" %>
<% end %>
