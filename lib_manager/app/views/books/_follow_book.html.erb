<%= form_for :following_book, url: user_follow_books_path(current_user),
  remote: true do |f| %>
  <%= hidden_field_tag :book_id, @book.id %>
  <%= f.submit t("views.books.shows.follow_book"), class: "btn btn-success" %>
<% end %>
