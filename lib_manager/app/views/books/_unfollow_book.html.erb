<%= form_for :unfollow_book, url: user_follow_book_path(current_user.id, @book.id),
  method: :delete, remote: true do |f| %>
  <%= hidden_field_tag :book_id, @book.id %>
  <%= f.submit t("views.books.shows.unfollow_book"), class: "btn btn-danger" %>
<% end %>
